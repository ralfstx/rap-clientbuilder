== Experimental client library builder for RWT ==

Usage:

  Class JsCompressor has a main method that recursively collects all *.js files
  in RWT_Q07/js/, compresses and concatenates them, and writes the resulting
  file to RWT_Q07/resources/client.js.
  RWT_Q07 is the root directory of the project org.eclipse.rap.rwt.q07,
  the path must be passed as first parameter.

YUI Compressor version 2.4.2 from:
  http://yuilibrary.com/downloads/#yuicompressor
